apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: gorax-worker-pdb
  labels:
    app: gorax
    component: worker
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: gorax
      component: worker
